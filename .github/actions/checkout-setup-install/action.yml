name: Checkout, Install and Setup
runs:
  using: "composite"
  steps:
    - name: Check out Git repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 22

    - name: Install Node.js dependencies
      run: npm ci
      id: install
