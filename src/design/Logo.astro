---
import logo from '../assets/logo.svg?raw';
import Icon from './Icon.astro';

interface Props {
  size?: string;
}

const { size = '1.5em' } = Astro.props;
---

<Icon
  html={logo}
  size={size}
/>
