---
import Icon from './Icon.astro';
import chevronDown from '../assets/icons/ion--chevron-down.svg?raw';

import { type ColorVariants, invertColorVariant } from '../util/color-variants';

interface Props {
  title?: string;
  variant?: ColorVariants;
  open?: boolean;
}
const isPrint = Astro.locals.isPrint;

const { title, variant = 'primary', open = isPrint } = Astro.props;
---

<style>
  .-primary {
    --accordion-border-color: var(--color-primary);
  }

  .-inverted-primary {
    --accordion-background-color: var(--color-primary);
    --accordion-summary-color: var(--color-primary);
  }

  .-secondary {
    --accordion-border-color: var(--color-secondary);
  }

  .-inverted-secondary {
    --accordion-background-color: var(--color-secondary);
    --accordion-summary-color: var(--color-secondary);
  }

  .-primary,
  .-secondary {
    --accordion-background-color: var(--color-surface);
    --accordion-summary-color: var(--color-surface);
    --accordion-padding: var(--vspace);
    --accordion-content-color: var(--accordion-border-color);
  }

  .-inverted-primary,
  .-inverted-secondary {
    --accordion-border-color: var(--color-surface-dark);
    --accordion-padding: var(--vspace-3);
    --accordion-max-width: fit-content;
    --accordion-content-color: var(--accordion-background-color);
  }

  .-primary,
  .-secondary,
  .-inverted-primary,
  .-inverted-secondary {
    --accordion-border-width: calc(var(--size-border) * 2);
  }

  .-is-print {
    &.-primary,
    &.-inverted-primary {
      --accordion-content-color: var(--color-primary);
      --accordion-summary-color: var(--color-primary);
    }

    &.-secondary,
    &.-inverted-secondary {
      --accordion-content-color: var(--color-secondary);
      --accordion-summary-color: var(--color-secondary);
    }

    --accordion-border-color: var(--color-surface);
    --accordion-background-color: var(--color-surface);
    --accordion-padding: 0;
    --accordion-border-width: 0;
  }

  details {
    border: var(--accordion-border-width) solid var(--accordion-border-color);
    max-width: var(--accordion-max-width);

    summary {
      padding: var(--accordion-padding);
      list-style: none;
      background-color: var(--accordion-border-color);
      color: var(--accordion-summary-color);
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: var(--vspace);

      h3 {
        margin: 0;
      }

      .icons {
        display: flex;
      }
    }

    /* accordions can be nested... but the styles will cascade */
    /* stylelint-disable no-descending-specificity */
    &[open] {
      summary {
        .icons {
          .chevron-icon {
            transform: rotate(180deg);
          }
        }
      }
    }

    article {
      padding: var(--accordion-padding);
      color: var(--accordion-content-color);

      details {
        .icons {
          .chevron-icon {
            /* accordions can be nested... but the styles will cascade */
            transform: unset !important;
          }
        }

        &[open] {
          .icons {
            .chevron-icon {
              /* accordions can be nested... but the styles will cascade */
              transform: rotate(180deg) !important;
            }
          }
        }
      }
    }
    /* stylelint-enable no-descending-specificity */
  }
</style>

<details
  open={open}
  class={`-${variant}${isPrint ? ' -is-print' : ''}`}
>
  <summary>
    {Astro.slots.has('summary') ? <slot name="summary" /> : <h3>{title}</h3>}
    {
      !isPrint && (
        <span class="icons">
          {Astro.slots.has('extra-icon') && (
            <span class="extra-icon">
              <slot name="extra-icon" />
            </span>
          )}
          <span class="chevron-icon">
            <Icon
              variant={invertColorVariant(variant)}
              html={chevronDown}
            />
          </span>
        </span>
      )
    }
  </summary>
  <article>
    <slot />
  </article>
</details>
