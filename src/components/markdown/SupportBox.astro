---
import Accordion from '../../design/Accordion.astro';

export interface Props {
  title: string;
}

const { title } = Astro.props;

Astro.locals.levelShift = 2;
---

<Accordion
  title={title}
  variant="secondary"
  level={1}
>
  <slot />
</Accordion>
