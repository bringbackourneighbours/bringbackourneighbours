---
import Accordion from '../../design/Accordion.astro';

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Accordion
  title={title}
  variant="secondary"
  level={3}
>
  <slot />
</Accordion>
